import { Message } from "../../entities/message-handler-types.js";
import { BotConfiguration } from "../../entities/bot-types.js";

// TODO: Add LONG TERM/SHORT TERM MEMORY here 
export function generatePrompt(config: BotConfiguration, recentMessages: Message[]) {
    
}